<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>403 - Unauthorized</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />
  <style>
    @keyframes shake {
      0% { transform: translateX(0); }
      25% { transform: translateX(-5px); }
      50% { transform: translateX(5px); }
      75% { transform: translateX(-5px); }
      100% { transform: translateX(0); }
    }
    .animate-shake {
      animation: shake 0.6s ease-in-out;
    }
  </style>
</head>
<body class="bg-white text-black flex items-center justify-center min-h-screen">
  <div class="bg-white border border-gray-200 rounded-2xl shadow-xl p-10 max-w-lg w-full text-center space-y-5">
    
    <!-- 403 Title -->
    <h1 class="text-6xl font-extrabold text-red-600 animate-shake">
      <i class="fas fa-lock text-red-400 mr-2"></i> 403
    </h1>

    <!-- Subheading -->
    <p class="text-2xl text-red-700 font-semibold">Access Denied</p>
    <p class="text-gray-600 text-base"><%= error || "You don't have permission to access this page." %></p>
    
    <!-- Back to Home Button -->
    <a href="/" class="inline-flex items-center gap-2 bg-red-600 text-white font-medium px-5 py-2 rounded-lg hover:bg-red-500 transition transform hover:scale-105 shadow">
      <i class="fas fa-arrow-left"></i> Back to Home
    </a>

    <!-- Divider -->
    <div class="border-t pt-4 mt-6 text-sm text-gray-500">
      <p>If you believe this is an error, contact our support team:</p>
      <a href="mailto:support@example.com" class="text-blue-600 hover:underline">
        <i class="fas fa-envelope"></i> support@example.com
      </a>
    </div>
  </div>
</body>
</html>
