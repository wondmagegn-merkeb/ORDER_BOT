<!DOCTYPE html>
<html lang="en" class="dark">
<head>
  <meta charset="UTF-8">
  <title>404 - Not Found</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />
  <style>
    @keyframes pulse {
      0% { transform: scale(1); opacity: 0.8; }
      50% { transform: scale(1.2); opacity: 1; }
      100% { transform: scale(1); opacity: 0.8; }
    }
    .animate-pulse-slow {
      animation: pulse 1.8s infinite;
    }
  </style>
<script src="https://cdn.tailwindcss.com"></script>

  <script>
    tailwind.config = {
      darkMode: 'class',
    };
if (localStorage.getItem('theme') === 'dark') {
      document.documentElement.classList.add('dark');
    }
  </script>
</head>
<body class="bg-white text-black dark:bg-gray-900 dark:text-white flex items-center justify-center min-h-screen transition-colors">
  <div class="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-2xl shadow-xl p-10 max-w-lg w-full text-center space-y-5">
    
    <!-- 404 Title -->
    <h1 class="text-6xl font-extrabold text-blue-600 dark:text-yellow-400 animate-pulse-slow">
      <i class="fas fa-exclamation-triangle mr-2"></i> 404
    </h1>

    <!-- Subheading -->
    <p class="text-2xl text-purple-700 dark:text-purple-300 font-semibold">Oops! Page Not Found</p>
    <p class="text-gray-600 dark:text-gray-300 text-base"><%= message || "The page you are looking for doesn't exist or has been moved." %></p>
    
    <!-- Back to Home Button -->
    <a href="/" class="inline-flex items-center gap-2 bg-purple-600 dark:bg-purple-500 text-white font-medium px-5 py-2 rounded-lg hover:bg-purple-500 dark:hover:bg-purple-400 transition transform hover:scale-105 shadow">
      <i class="fas fa-arrow-left"></i> Back to Home
    </a>

    <!-- Divider -->
    <div class="border-t border-gray-300 dark:border-gray-600 pt-4 mt-6 text-sm text-gray-500 dark:text-gray-400">
      <p>If you need help, contact our support team:</p>
      <a href="mailto:support@example.com" class="text-blue-600 dark:text-blue-400 hover:underline">
        <i class="fas fa-envelope"></i> support@example.com
      </a>
    </div>
  </div>
</body>
</html>
