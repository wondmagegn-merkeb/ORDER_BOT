<div class="max-w-xl mx-auto bg-white p-6 rounded-lg shadow-md">
  <h2 class="text-3xl font-extrabold mb-6 text-gray-800 text-center flex items-center justify-center gap-2">
    <i class="fas fa-user-edit text-green-600"></i>
    Update User
  </h2>

  <form action="/api/users/<%= user.userId %>?_method=PUT" method="POST" class="space-y-5">
    <!-- Hidden Username -->
    <input type="hidden" name="username" value="<%= user.username %>">

    <!-- Hidden Telegram ID -->
    <input type="hidden" name="telegramId" value="<%= user.telegramId %>">

    <!-- User Type Selection -->
    <div>
      <label class="block mb-1 text-gray-700 font-medium">User Type</label>
      <div class="flex items-center border border-gray-300 p-2 rounded">
        <i class="fas fa-user-tag text-gray-400 mr-2"></i>
        <select name="userType" class="w-full bg-white focus:outline-none">
          <option value="guest" <%= user.userType === 'guest' ? 'selected' : '' %>>Guest</option>
          <option value="vip" <%= user.userType === 'vip' ? 'selected' : '' %>>VIP</option>
          <option value="customer" <%= user.userType === 'customer' ? 'selected' : '' %>>Customer</option>
        </select>
      </div>
    </div>

    <!-- State (Single Active Checkbox) -->
    <div>
      <label class="block mb-1 text-gray-700 font-medium">State</label>
      <label class="inline-flex items-center">
        <input type="checkbox" name="states" value="active" <%= user.states === 'active' ? 'checked' : '' %> class="form-checkbox text-green-600">
        <span class="ml-2 text-gray-700">Active</span>
      </label>
    </div>

    <!-- Submit and Cancel Buttons -->
    <div class="pt-6 flex justify-between items-center">
      <button type="submit" class="bg-green-600 text-white px-6 py-2 rounded flex items-center gap-2 hover:bg-green-700 transition font-semibold">
        <i class="fas fa-save"></i>
        Update User
      </button>

      <a href="/admin/users" class="text-gray-600 hover:text-red-500 transition flex items-center gap-2 font-medium">
        <i class="fas fa-times-circle text-red-500"></i>
        Cancel
      </a>
    </div>
  </form>
</div>
